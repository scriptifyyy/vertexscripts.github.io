<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Vertex Scripts</title>

  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 128 128'%3E%3Cdefs%3E%3ClinearGradient id='g' x1='0' y1='0' x2='1' y2='1'%3E%3Cstop offset='0%25' stop-color='%23f2fffb'/%3E%3Cstop offset='45%25' stop-color='%238df0d1'/%3E%3Cstop offset='100%25' stop-color='%232ea881'/%3E%3C/linearGradient%3E%3Cfilter id='glow' x='-50%25' y='-50%25' width='200%25' height='200%25'%3E%3CfeGaussianBlur stdDeviation='6' result='b'/%3E%3CfeMerge%3E%3CfeMergeNode in='b'/%3E%3CfeMergeNode in='SourceGraphic'/%3E%3C/feMerge%3E%3C/filter%3E%3C/defs%3E%3Crect width='128' height='128' rx='28' fill='%23081412'/%3E%3Cg filter='url(%23glow)'%3E%3Cpath d='M20 22h28l16 30 16-30h28L64 108 20 22Z' fill='url(%23g)'/%3E%3C/g%3E%3Cpath d='M42 22h20l2 6-8 16-10-16-4-6Z' fill='%230b1715' opacity='.78'/%3E%3C/svg%3E" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />

  <style>
    :root {
      --primary: #46c7a0;
      --primary-2: #2ea881;
      --primary-3: #8df0d1;
      --text: #f3fffb;
      --muted: #9cc5bc;
      --muted-2: #6ea293;
      --success: #4af59a;
      --danger: #ffb8c3;
      --warn: #ffe39b;
      --shadow: 0 28px 70px rgba(0, 0, 0, 0.42);
      --radius-xl: 34px;
      --maxw: 1220px;
    }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }

    body {
      margin: 0;
      font-family: "Inter", system-ui, sans-serif;
      color: var(--text);
      background:
        radial-gradient(circle at 14% 10%, rgba(46, 168, 129, 0.14), transparent 20%),
        radial-gradient(circle at 90% 70%, rgba(103, 226, 197, 0.12), transparent 28%),
        linear-gradient(118deg, #020717 0%, #031139 48%, #041120 100%);
      overflow-x: hidden;
      min-height: 100vh;
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      opacity: 0.12;
      background-image: radial-gradient(circle at center, rgba(173,255,232,0.7) 0 1px, transparent 1.2px);
      background-size: 34px 34px;
      mask-image: linear-gradient(to bottom, rgba(255,255,255,.85), rgba(255,255,255,.22));
    }

    body::after {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      background: radial-gradient(420px 420px at var(--mx, 78%) var(--my, 22%), rgba(96,255,207,0.08), transparent 36%);
      z-index: 0;
    }

    a { color: inherit; text-decoration: none; }
    img { display: block; max-width: 100%; }
    button, input { font: inherit; }

    .wrap {
      width: min(var(--maxw), calc(100% - 28px));
      margin: 0 auto;
      position: relative;
      z-index: 1;
    }

    .glass {
      background: linear-gradient(180deg, rgba(6,14,42,0.92), rgba(5,12,34,0.94));
      border: 1px solid rgba(70,199,160,0.14);
      box-shadow: var(--shadow);
      backdrop-filter: blur(18px);
      border-radius: var(--radius-xl);
      position: relative;
      overflow: hidden;
    }

    .glass::after {
      content: "";
      position: absolute;
      inset: auto -36% -62% auto;
      width: 220px;
      height: 220px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(70,199,160,0.14), transparent 60%);
      pointer-events: none;
    }

    .topbar {
      position: sticky;
      top: 14px;
      z-index: 30;
      padding-top: 14px;
    }

    .navbar {
      min-height: 72px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      padding: 12px 18px;
      border-radius: 999px;
      background: rgba(6,15,38,0.72);
      border: 1px solid rgba(70,199,160,0.14);
      backdrop-filter: blur(18px);
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 12px;
      font-weight: 900;
      letter-spacing: -0.04em;
      font-size: 1.12rem;
    }

    .brand em {
      color: var(--primary-3);
      font-style: italic;
    }

    .brand-mark,
    .hero-logo {
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(11,26,30,1), rgba(7,16,14,1));
      border: 1px solid rgba(106,244,201,0.18);
      box-shadow: 0 0 40px rgba(79,224,177,0.16);
      overflow: hidden;
      transition: box-shadow .3s ease, transform .3s ease, border-color .3s ease;
    }

    .brand-mark { width: 46px; height: 46px; border-radius: 16px; }
    .hero-logo { width: 130px; height: 130px; margin: 0 auto 28px; border-radius: 34px; }

    .brand-mark:hover,
    .hero-logo:hover {
      transform: translateY(-2px) scale(1.02);
      border-color: rgba(141,240,209,0.4);
      box-shadow: 0 0 32px rgba(103,226,197,0.24), 0 0 72px rgba(70,199,160,0.16);
    }

    .logo-svg {
      width: 28px;
      height: 28px;
      display: block;
      filter: drop-shadow(0 0 8px rgba(103,226,197,0.22));
      animation: logoPulse 4.2s ease-in-out infinite;
    }

    .hero-logo .logo-svg {
      width: 72px;
      height: 72px;
      filter: drop-shadow(0 0 18px rgba(103,226,197,0.28)) drop-shadow(0 0 38px rgba(70,199,160,0.16));
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 10px;
      flex-wrap: wrap;
    }

    .nav-links a {
      color: #bbddd5;
      padding: 10px 14px;
      border-radius: 999px;
      font-weight: 700;
      font-size: .95rem;
      transition: .2s ease;
    }

    .nav-links a:hover,
    .nav-links a.active {
      color: white;
      background: rgba(70,199,160,0.12);
    }

    .nav-actions { display: flex; gap: 10px; }

    .icon-btn,
    .menu-btn,
    .back-top {
      width: 44px;
      height: 44px;
      border-radius: 14px;
      border: 1px solid rgba(70,199,160,0.14);
      background: rgba(14,31,27,0.82);
      color: white;
      cursor: pointer;
      transition: transform .2s ease, opacity .2s ease;
    }

    .icon-btn:hover,
    .menu-btn:hover,
    .back-top:hover { transform: translateY(-2px); }

    .menu-btn { display: none; }

    .hero {
      min-height: calc(100vh - 44px);
      display: grid;
      place-items: center;
      text-align: center;
      padding: 68px 0 78px;
    }

    .hero-card { width: min(960px, 100%); padding: 48px 28px 28px; }

    .eyebrow,
    .pill,
    .badge,
    .tag-filter {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      border-radius: 999px;
      font-weight: 800;
    }

    .eyebrow {
      padding: 9px 14px;
      color: #bdf3e4;
      background: rgba(70,199,160,0.1);
      border: 1px solid rgba(70,199,160,0.12);
      text-transform: uppercase;
      letter-spacing: .12em;
      font-size: .8rem;
      margin-bottom: 18px;
    }

    .dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: var(--success);
      box-shadow: 0 0 14px rgba(74,245,154,0.78);
    }

    .hero-title {
      margin: 0;
      font-size: clamp(3.2rem, 8vw, 5.7rem);
      line-height: .9;
      letter-spacing: -.08em;
      font-style: italic;
      font-weight: 900;
    }

    .hero-title .accent {
      color: var(--primary-3);
      text-shadow: 0 0 24px rgba(141,240,209,0.18);
    }

    .hero-subtitle {
      width: min(760px, 100%);
      margin: 18px auto 0;
      font-weight: 700;
      letter-spacing: .04em;
      font-size: clamp(1.05rem, 1.9vw, 1.2rem);
      background: linear-gradient(90deg, #8df0d1, #46c7a0, #b8fff0);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 0 0 18px rgba(70,199,160,0.25);
      animation: glowText 3s ease-in-out infinite alternate;
    }

    .hero-meta {
      margin: 16px 0 18px;
      color: #89b3a8;
      text-transform: uppercase;
      letter-spacing: .3em;
      font-size: .83rem;
      font-style: italic;
      font-weight: 800;
    }

    .ticker-bar {
      width: min(860px, 100%);
      margin: 0 auto 24px;
      overflow: hidden;
      border-radius: 999px;
      border: 1px solid rgba(70,199,160,0.14);
      background: rgba(9,22,20,0.76);
      box-shadow: inset 0 0 0 1px rgba(70,199,160,0.04);
    }

    .ticker-track {
      display: flex;
      gap: 34px;
      width: max-content;
      padding: 12px 22px;
      color: #d8fff3;
      font-size: .88rem;
      font-weight: 800;
      white-space: nowrap;
      animation: tickerMove 18s linear infinite;
    }

    .ticker-track span { opacity: .92; }

    .hero-actions,
    .filter-row,
    .stats-row,
    .btn-row,
    .badge-row,
    .social-btn-row {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
    }

    .hero-actions { margin-bottom: 26px; }

    .hero-cta,
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      border: 0;
      color: white;
      cursor: pointer;
      font-weight: 800;
      padding: 16px 22px;
      border-radius: 18px;
      background: linear-gradient(90deg, var(--primary-2), var(--primary));
      box-shadow: 0 16px 34px rgba(46,168,129,0.22);
      transition: transform .2s ease, filter .2s ease, box-shadow .2s ease;
      position: relative;
      overflow: hidden;
    }

    .hero-cta::after,
    .btn::after {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(120deg, transparent 0%, rgba(255,255,255,0.18) 40%, transparent 65%);
      transform: translateX(-120%);
      transition: transform .5s ease;
    }

    .hero-cta:hover::after,
    .btn:hover::after { transform: translateX(120%); }

    .hero-cta.ghost,
    .btn.secondary {
      background: rgba(16,38,33,0.84);
      border: 1px solid rgba(70,199,160,0.16);
      box-shadow: none;
    }

    .searchbar {
      width: min(640px, 100%);
      margin: 0 auto 22px;
      position: relative;
    }

    .searchbar input {
      width: 100%;
      height: 66px;
      border-radius: 999px;
      border: 1px solid rgba(70,199,160,0.36);
      background: rgba(8,22,19,0.9);
      color: white;
      padding: 0 70px 0 24px;
      outline: none;
      box-shadow: inset 0 0 0 1px rgba(70,199,160,0.04), 0 0 30px rgba(46,168,129,0.08);
    }

    .searchbar button {
      position: absolute;
      right: 10px;
      top: 50%;
      transform: translateY(-50%);
      width: 48px;
      height: 48px;
      border-radius: 999px;
      border: 0;
      color: white;
      background: linear-gradient(180deg, var(--primary), var(--primary-2));
      cursor: pointer;
    }

    .tag-filter {
      padding: 10px 14px;
      background: rgba(16,38,33,0.84);
      border: 1px solid rgba(70,199,160,0.12);
      color: #c3efe3;
      cursor: pointer;
      transition: .2s ease;
    }

    .tag-filter.active { background: rgba(70,199,160,0.14); color: white; }

    .stats-row { margin-top: 10px; }

    .stat {
      min-width: 150px;
      padding: 16px 18px;
      border-radius: 22px;
      background: rgba(10,27,24,0.72);
      border: 1px solid rgba(70,199,160,0.1);
      text-align: left;
      transition: transform .25s ease, border-color .25s ease;
    }

    .stat:hover { transform: translateY(-3px); border-color: rgba(70,199,160,0.24); }

    .counter {
      font-size: 1.35rem;
      font-weight: 900;
      display: block;
      margin-bottom: 4px;
    }

    .stat span { color: var(--muted); font-size: .92rem; }

    section { padding: 24px 0 54px; position: relative; z-index: 1; }

    .section-head {
      display: flex;
      align-items: center;
      gap: 16px;
      margin-bottom: 24px;
    }

    .section-head h2 {
      margin: 0;
      white-space: nowrap;
      font-size: clamp(1.9rem, 3vw, 3rem);
      line-height: 1;
      font-style: italic;
      letter-spacing: -.06em;
    }

    .section-head .line {
      width: 100%;
      height: 2px;
      border-radius: 999px;
      background: linear-gradient(90deg, rgba(70,199,160,0.7), transparent 86%);
      opacity: .9;
    }

    .grid { display: grid; gap: 30px; }
    .cards-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }

    .card {
      padding: 28px;
      min-height: 100%;
      overflow: hidden;
      position: relative;
      transition: transform .25s ease, border-color .25s ease, box-shadow .25s ease, filter .25s ease;
      transform-style: preserve-3d;
    }

    .spotlight::before {
      content: "";
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at var(--sx, 50%) var(--sy, 50%), rgba(70,199,160,0.12), transparent 34%);
      opacity: 0;
      transition: opacity .24s ease;
      pointer-events: none;
    }

    .spotlight:hover::before { opacity: 1; }
    .card:hover {
      transform: translateY(-6px) scale(1.01);
      border-color: rgba(70,199,160,0.24);
      box-shadow: 0 30px 80px rgba(0,0,0,0.48);
    }

    .top-row {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 20px;
    }

    .app-icon {
      width: 62px;
      height: 62px;
      border-radius: 18px;
      background: linear-gradient(180deg, rgba(19,45,39,1), rgba(8,18,16,1));
      border: 1px solid rgba(70,199,160,0.16);
      box-shadow: 0 10px 22px rgba(0,0,0,0.18);
      overflow: hidden;
      flex-shrink: 0;
      object-fit: cover;
      position: relative;
    }

    .app-icon::after {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(255,255,255,0.12), transparent 55%);
      pointer-events: none;
    }

    .app-icon img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .icon-letter {
      display: grid;
      place-items: center;
      color: white;
      font-weight: 900;
      font-size: 1.25rem;
      height: 100%;
    }

    .pill,
    .badge {
      padding: 9px 13px;
      background: rgba(16,38,33,0.88);
      border: 1px solid rgba(70,199,160,0.16);
      color: #c1f3e6;
      font-size: .78rem;
      text-transform: uppercase;
      letter-spacing: .06em;
    }

    .status {
      display: flex;
      align-items: center;
      gap: 10px;
      color: #74ffb6;
      font-size: .82rem;
      font-weight: 800;
      text-transform: uppercase;
      margin-bottom: 14px;
    }

    .status.undetected { color: #8effc4; }
    .status.testing { color: var(--warn); }
    .status.patched { color: var(--danger); }

    .card h3 {
      margin: 0 0 18px;
      font-size: clamp(1.9rem, 2.6vw, 2.9rem);
      line-height: .95;
      letter-spacing: -.06em;
      font-style: italic;
    }

    .muted { color: #bddfd7; line-height: 1.7; }

    .code {
      background: rgba(5,14,12,0.94);
      border: 1px solid rgba(70,199,160,0.08);
      color: #9ef3d8;
      border-radius: 14px;
      padding: 15px 16px;
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: .8rem;
      overflow: hidden;
      white-space: nowrap;
      text-overflow: ellipsis;
      margin-bottom: 16px;
    }

    .badge-row { justify-content: flex-start; margin-bottom: 16px; }
    .badge.version { background: rgba(25,48,43,0.92); color: #d9fff3; }
    .badge.dev { background: rgba(40,36,14,0.9); border-color: rgba(235,203,91,0.18); color: #ffeeb2; }
    .btn-row { justify-content: flex-start; margin-top: auto; }
    .btn { flex: 1 1 160px; }

    .script-meta {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      align-items: center;
      margin: 0 0 14px;
      color: #cdeee4;
      font-size: .88rem;
      font-weight: 700;
    }

    .script-meta span {
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(16,38,33,0.7);
      border: 1px solid rgba(70,199,160,0.12);
    }

    .updated-time {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin: 0 0 14px;
      color: #aee8d8;
      font-size: .86rem;
      font-weight: 700;
    }

    .trust-row {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin: 0 0 14px;
    }

    .trust-chip {
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(16,38,33,0.7);
      border: 1px solid rgba(70,199,160,0.12);
      color: #d8fff3;
      font-size: .8rem;
      font-weight: 800;
    }

    .modal-link {
      width: 100%;
      display: inline-flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      padding: 14px 16px;
      margin-top: 12px;
      border-radius: 16px;
      border: 1px solid rgba(70,199,160,0.12);
      background: rgba(8,22,19,0.9);
      color: #c8fff0;
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: .82rem;
      text-decoration: none;
    }

    .card-badges-top {
      position: absolute;
      top: 18px;
      right: 18px;
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      z-index: 2;
    }

    .micro-badge {
      padding: 7px 10px;
      border-radius: 999px;
      font-size: .72rem;
      font-weight: 900;
      text-transform: uppercase;
      letter-spacing: .06em;
      border: 1px solid rgba(70,199,160,0.14);
      background: rgba(10,24,20,0.86);
      color: #eafff8;
      box-shadow: 0 0 18px rgba(70,199,160,0.08);
    }

    .micro-badge.trending { color: #ffe7a9; border-color: rgba(255,214,102,0.2); }

    .social-card {
      text-align: center;
      padding-top: 40px;
      min-height: 280px;
    }

    .social-icon-wrap {
      width: 88px;
      height: 88px;
      margin: 0 auto 24px;
      border-radius: 999px;
      display: grid;
      place-items: center;
    }

    .social-icon-wrap.youtube {
      background: rgba(255,57,57,0.12);
      border: 1px solid rgba(255,57,57,0.25);
      box-shadow: 0 0 32px rgba(255,57,57,0.14);
    }

    .social-icon-wrap.tiktok {
      background: rgba(255,54,170,0.12);
      border: 1px solid rgba(255,54,170,0.25);
      box-shadow: 0 0 32px rgba(255,54,170,0.14);
    }

    .social-btn-row { justify-content: center; }

    .admin-modal {
      position: fixed;
      inset: 0;
      background: rgba(4,10,9,0.72);
      backdrop-filter: blur(10px);
      display: none;
      align-items: center;
      justify-content: center;
      padding: 18px;
      z-index: 90;
    }

    .admin-modal.show { display: flex; }

    .admin-panel { display: block; }

    .admin-modal-card {
      width: min(860px, 100%);
      max-height: min(88vh, 920px);
      overflow: auto;
      padding: 24px;
      position: relative;
    }

    .admin-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 18px;
      margin-bottom: 20px;
    }

    .admin-stat {
      padding: 20px;
      border-radius: 22px;
      background: rgba(10,27,24,0.72);
      border: 1px solid rgba(70,199,160,0.1);
    }

    .admin-stat strong {
      display: block;
      font-size: 1.6rem;
      margin-bottom: 6px;
    }

    .admin-stat span {
      color: var(--muted);
      font-size: .92rem;
    }

    .admin-list {
      display: grid;
      gap: 12px;
      margin-top: 18px;
    }

    .admin-row {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      align-items: center;
      padding: 14px 16px;
      border-radius: 16px;
      background: rgba(9,22,20,0.68);
      border: 1px solid rgba(70,199,160,0.08);
      color: #d9fff3;
    }

    .admin-auth-note {
      color: #bde3d7;
      margin: 0 0 18px;
      line-height: 1.7;
    }

    .admin-form {
      display: grid;
      gap: 12px;
    }

    .admin-form input {
      width: 100%;
      padding: 14px 16px;
      border-radius: 14px;
      border: 1px solid rgba(70,199,160,0.16);
      background: rgba(8,22,19,0.92);
      color: white;
      outline: none;
    }

    .admin-helper {
      color: #97cfc0;
      font-size: .88rem;
      line-height: 1.6;
    }

    .admin-pin-row {
      display: grid;
      gap: 12px;
      margin-bottom: 14px;
    }

    .admin-loading {
      display: none;
      align-items: center;
      gap: 12px;
      margin-top: 8px;
      color: #c8efe3;
      font-weight: 700;
    }

    .admin-loading.show { display: inline-flex; }

    .spinner {
      width: 18px;
      height: 18px;
      border-radius: 50%;
      border: 2px solid rgba(141,240,209,0.22);
      border-top-color: #8df0d1;
      animation: spin .8s linear infinite;
    }

    .footer-brand {
      padding: 28px 30px;
      text-align: center;
      color: #aad0c7;
      display: flex;
      flex-direction: column;
      gap: 6px;
      align-items: center;
      justify-content: center;
    }

    .footer-brand strong {
      color: #dffff6;
      font-size: 1rem;
      letter-spacing: .04em;
      text-shadow: 0 0 18px rgba(70,199,160,0.16);
    }

    .footer-brand small {
      color: #7fb4a6;
      text-transform: uppercase;
      letter-spacing: .14em;
      font-size: .75rem;
    }

    .live-feed {
      position: fixed;
      left: 18px;
      bottom: 22px;
      z-index: 55;
      max-width: 320px;
      pointer-events: none;
    }

    .live-feed-item {
      background: rgba(8,20,18,0.96);
      border: 1px solid rgba(70,199,160,0.16);
      border-radius: 18px;
      box-shadow: var(--shadow);
      padding: 14px 16px;
      color: #ecfff9;
      opacity: 0;
      transform: translateY(18px);
      transition: .3s ease;
      font-size: .92rem;
      line-height: 1.45;
    }

    .live-feed-item.show {
      opacity: 1;
      transform: translateY(0);
    }

    .delta-indicator {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      margin-left: 8px;
      font-size: .8rem;
      font-weight: 800;
      opacity: .9;
    }

    .delta-up { color: #8effc4; }
    .delta-down { color: #ffb8c3; }

    .toast {
      position: fixed;
      left: 50%;
      bottom: 28px;
      transform: translateX(-50%) translateY(18px);
      background: rgba(8,20,18,0.98);
      border: 1px solid rgba(70,199,160,0.16);
      color: white;
      border-radius: 22px;
      padding: 14px 18px;
      min-width: 320px;
      display: flex;
      align-items: center;
      gap: 14px;
      box-shadow: var(--shadow);
      opacity: 0;
      pointer-events: none;
      transition: .28s ease;
      z-index: 60;
    }

    .toast.show {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }

    .toast-badge {
      width: 42px;
      height: 42px;
      border-radius: 14px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, var(--primary-2), var(--primary));
      font-size: 1.1rem;
    }

    .back-top {
      position: fixed;
      right: 18px;
      bottom: 22px;
      opacity: 0;
      pointer-events: none;
      transform: translateY(8px);
      z-index: 40;
    }

    .back-top.show {
      opacity: 1;
      pointer-events: auto;
      transform: translateY(0);
    }

    .reveal {
      opacity: 0;
      transform: translateY(24px);
      transition: opacity .7s ease, transform .7s ease;
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .hidden { display: none !important; }

    .modal {
      position: fixed;
      inset: 0;
      background: rgba(4,10,9,0.68);
      backdrop-filter: blur(8px);
      display: none;
      align-items: center;
      justify-content: center;
      padding: 18px;
      z-index: 70;
    }

    .modal-card {
      width: min(720px, 100%);
      padding: 24px 24px 20px;
      position: relative;
    }

    .close-btn {
      position: absolute;
      top: 16px;
      right: 16px;
      width: 42px;
      height: 42px;
      border-radius: 14px;
      border: 1px solid rgba(70,199,160,0.16);
      background: rgba(12,28,24,0.92);
      color: white;
      cursor: pointer;
      z-index: 8;
    }

    @keyframes logoPulse {
      0%, 100% { filter: drop-shadow(0 0 8px rgba(103,226,197,0.22)); }
      50% { filter: drop-shadow(0 0 14px rgba(141,240,209,0.34)) drop-shadow(0 0 26px rgba(70,199,160,0.18)); }
    }

    @keyframes glowText {
      0% { filter: drop-shadow(0 0 6px rgba(141,240,209,0.25)); }
      100% { filter: drop-shadow(0 0 16px rgba(141,240,209,0.55)); }
    }

    @keyframes tickerMove {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }

    @keyframes pageEnter {
      0% { opacity: 0; transform: translateY(12px) scale(.99); }
      100% { opacity: 1; transform: translateY(0) scale(1); }
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    @media (prefers-reduced-motion: reduce) {
      .logo-svg,
      .hero-subtitle,
      .spinner,
      .ticker-track { animation: none; }
    }

    @media (max-width: 900px) {
      .cards-2,
      .socials-grid,
      .admin-grid {
        grid-template-columns: 1fr !important;
      }
    }

    @media (max-width: 860px) {
      .nav-links {
        display: none;
        position: absolute;
        top: calc(100% + 12px);
        left: 0;
        right: 0;
        padding: 14px;
        border-radius: 24px;
        background: rgba(9,22,20,0.98);
        border: 1px solid rgba(70,199,160,0.14);
        flex-direction: column;
        align-items: stretch;
      }

      .nav-links.open { display: flex; }
      .menu-btn { display: inline-grid; place-items: center; }
      .navbar { position: relative; }
    }

    @media (max-width: 640px) {
      .wrap { width: min(var(--maxw), calc(100% - 18px)); }
      .hero-card { padding: 32px 14px 16px; }
      .hero-meta { letter-spacing: .14em; }
      .stat { width: 100%; }
      .toast { min-width: 0; width: calc(100% - 22px); }
      .hero-cta, .btn { width: 100%; }
      .btn-row, .social-btn-row { flex-direction: column; }
      .card { padding: 22px; }
    }
  </style>
</head>
<body>
  <div class="topbar wrap">
    <nav class="navbar glass">
      <a href="#home" class="brand">
        <div class="brand-mark">
          <svg class="logo-svg" viewBox="0 0 128 128" xmlns="http://www.w3.org/2000/svg" aria-label="Vertex logo">
            <defs>
              <linearGradient id="vertexBossNav" x1="18" y1="14" x2="108" y2="112" gradientUnits="userSpaceOnUse">
                <stop offset="0%" stop-color="#f2fffb"/>
                <stop offset="42%" stop-color="#8df0d1"/>
                <stop offset="100%" stop-color="#2ea881"/>
              </linearGradient>
              <filter id="vertexBossGlowNav" x="-50%" y="-50%" width="200%" height="200%">
                <feGaussianBlur stdDeviation="6" result="b"/>
                <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
              </filter>
            </defs>
            <rect width="128" height="128" rx="28" fill="#081412"/>
            <g filter="url(#vertexBossGlowNav)">
              <path d="M20 22h28l16 30 16-30h28L64 108 20 22Z" fill="url(#vertexBossNav)"/>
            </g>
            <path d="M42 22h20l2 6-8 16-10-16-4-6Z" fill="#0b1715" opacity=".78"/>
          </svg>
        </div>
        <span>VERTEX <em>SCRIPTS</em></span>
      </a>

      <div class="nav-links" id="navLinks">
        <a href="#home" class="active">Home</a>
        <a href="#latest-scripts">Scripts</a>
        <a href="#executors">Executors</a>
        <a href="#socials">Socials</a>
      </div>

      <div class="nav-actions">
        <button class="icon-btn" id="adminBtn" aria-label="Admin">⚙</button>
        <button class="icon-btn" id="themeToggle" aria-label="Theme">◐</button>
        <button class="menu-btn" id="menuBtn" aria-label="Menu">☰</button>
      </div>
    </nav>
  </div>

  <header class="hero wrap reveal" id="home">
    <div class="hero-card glass">
      <div class="hero-logo">
        <svg class="logo-svg" viewBox="0 0 128 128" xmlns="http://www.w3.org/2000/svg" aria-label="Vertex logo">
          <defs>
            <linearGradient id="vertexBossHero" x1="18" y1="14" x2="108" y2="112" gradientUnits="userSpaceOnUse">
              <stop offset="0%" stop-color="#f7fffd"/>
              <stop offset="42%" stop-color="#8df0d1"/>
              <stop offset="100%" stop-color="#2ea881"/>
            </linearGradient>
            <filter id="vertexBossGlowHero" x="-50%" y="-50%" width="200%" height="200%">
              <feGaussianBlur stdDeviation="7" result="b"/>
              <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
            </filter>
          </defs>
          <rect width="128" height="128" rx="28" fill="#081412"/>
          <g filter="url(#vertexBossGlowHero)">
            <path d="M20 22h28l16 30 16-30h28L64 108 20 22Z" fill="url(#vertexBossHero)"/>
          </g>
          <path d="M42 22h20l2 6-8 16-10-16-4-6Z" fill="#0b1715" opacity=".78"/>
        </svg>
      </div>

      <div class="eyebrow"><span class="dot"></span> Clean • Fast • Premium Script Hub</div>
      <h1 class="hero-title">VERTEX <span class="accent">SCRIPTS</span></h1>
      <p class="hero-subtitle">Working &amp; Tested Scripts 🌟</p>

<style>
.ticker-bar {
  overflow: hidden;
  width: 100%;
  background: rgba(8,20,18,0.6);
  border-top: 1px solid rgba(70,199,160,0.15);
  border-bottom: 1px solid rgba(70,199,160,0.15);
  margin: 18px 0;
  border-radius: 12px;
  mask-image: linear-gradient(to right, transparent, white 10%, white 90%, transparent);
}

.ticker-track {
  display: flex;
  gap: 28px;
  width: max-content;
  animation: tickerScroll 22s linear infinite;
}

.ticker-track span {
  white-space: nowrap;
  font-weight: 600;
  font-size: 0.9rem;
  color: #cffff1;
  padding: 8px 0;
  opacity: 0.9;
}

/* 🔥 glow pulse effect */
.pulse {
  color: #8effc4;
  animation: glowPulse 1.8s ease-in-out infinite;
}

/* animation */
@keyframes tickerScroll {
  0% { transform: translateX(0%); }
  100% { transform: translateX(-50%); }
}

/* glow animation */
@keyframes glowPulse {
  0%, 100% {
    text-shadow: 0 0 6px rgba(70,199,160,0.4),
                 0 0 12px rgba(70,199,160,0.2);
  }
  50% {
    text-shadow: 0 0 12px rgba(70,199,160,0.9),
                 0 0 24px rgba(70,199,160,0.5);
  }
}

/* pause on hover */
.ticker-bar:hover .ticker-track {
  animation-play-state: paused;
}
</style>

<div class="ticker-bar">
  <div class="ticker-track">
    <span>🛠️ MM2 Script Fixed</span>
    <span>⭐ Adopt Me Script Updated</span>
    <span class="pulse">🔥 Blade Ball Trending</span>
    <span>🚧 PS99 In Development</span>
    <span class="pulse">⚡ Steal A Brainrot Active</span>

    <!-- duplicate for smooth loop -->
    <span>🛠️ MM2 Script Fixed</span>
    <span>⭐ Adopt Me Script Updated</span>
    <span class="pulse">🔥 Blade Ball Trending</span>
    <span>🚧 PS99 In Development</span>
    <span class="pulse">⚡ Steal A Brainrot Active</span>
  </div>
</div>
      <div class="hero-actions">
        <a href="#latest-scripts" class="hero-cta">Browse Scripts</a>
        <a href="#latest-scripts" class="hero-cta ghost">Latest Scripts</a>
      </div>

      <div class="searchbar">
        <input id="searchInput" type="text" placeholder="Search latest scripts..." />
        <button aria-label="Search">⌕</button>
      </div>

      <div class="filter-row">
        <button class="tag-filter active" data-filter="all">All</button>
        <button class="tag-filter" data-filter="script">Scripts</button>
        <button class="tag-filter" data-filter="executor">Executors</button>
      </div>

      <div class="stats-row">
        <div class="stat glass"><strong class="counter" data-target="5">0</strong><span>Latest scripts</span></div>
        <div class="stat glass"><strong class="counter" id="heroVisitorCount" data-target="248">0</strong><span>Visitors online</span></div>
        <div class="stat glass"><strong class="counter" data-target="98">0</strong><span>Uptime rating</span></div>
        <div class="stat glass"><strong class="counter" data-target="245">0</strong><span>Community reviews</span></div>
      </div>
    </div>
  </header>

  <main class="wrap">
    <section class="reveal" id="latest-scripts">
      <div class="section-head">
        <h2><span style="color: var(--primary-3);">Latest</span> Scripts</h2>
        <div class="line"></div>
      </div>

      <div class="grid cards-2">
        <article class="card glass spotlight searchable"
          data-name="mm2 script murder mystery 2"
          data-category="script"
          data-title="MM2 Script"
          data-detail="loadstring(game:HttpGet('https://raw.githubusercontent.com/rysscripts/mm2/refs/heads/main/loader.lua'))()"
          data-copy="loadstring(game:HttpGet('https://raw.githubusercontent.com/rysscripts/mm2/refs/heads/main/loader.lua'))()">
          <div class="card-badges-top"><span class="micro-badge">NEW</span><span class="micro-badge trending">TRENDING</span></div>
          <div class="top-row">
            <div class="app-icon">
              <!-- replace with your real image -->
              <img src="images/mm2.png" alt="MM2 Script" onerror="this.style.display='none'; this.parentElement.innerHTML='<div class=&quot;icon-letter&quot;>MM</div>';">
            </div>
            <span class="pill">MM2</span>
          </div>
          <div class="status"><span class="dot"></span> Working</div>
          <h3>MM2 Script</h3>
          <div class="badge-row">
            <span class="badge version">v4.8.2</span>
            <span class="badge">Verified</span>
            <span class="badge">Updated Today</span>
          </div>
          <div class="updated-time">⏱️ Updated 2 hours ago</div>
          <div class="script-meta"><span>👁️ 1.8K views</span><span>⭐ 4.9 rating</span></div>
          <div class="code">loadstring(game:HttpGet('https://raw.githubusercontent.com/rysscripts/mm2/refs/heads/main/loader.lua'))()</div>
          <div class="btn-row">
            <button class="btn copy-btn" data-script-name="MM2 Script" data-copy="loadstring(game:HttpGet('https://raw.githubusercontent.com/rysscripts/mm2/refs/heads/main/loader.lua'))()">Copy Script</button>
            <button class="btn secondary open-modal">Quick View</button>
          </div>
        </article>

        <article class="card glass spotlight searchable"
          data-name="adopt me script adopt me"
          data-category="script"
          data-title="Adopt Me Script"
          data-detail="loadstring(game:HttpGet('https://raw.githubusercontent.com/rysscripts/adoptme/refs/heads/main/loader.lua'))()"
          data-copy="loadstring(game:HttpGet('https://raw.githubusercontent.com/rysscripts/adoptme/refs/heads/main/loader.lua'))()">
          <div class="card-badges-top"><span class="micro-badge">NEW</span><span class="micro-badge trending">TRENDING</span></div>
          <div class="top-row">
            <div class="app-icon">
              <img src="images/adoptme.png" alt="Adopt Me Script" onerror="this.style.display='none'; this.parentElement.innerHTML='<div class=&quot;icon-letter&quot;>AM</div>';">
            </div>
            <span class="pill">ADM</span>
          </div>
          <div class="status"><span class="dot"></span> Working</div>
          <h3>Adopt Me Script</h3>
          <div class="badge-row">
            <span class="badge version">v2.1.0</span>
            <span class="badge">Popular</span>
            <span class="badge">Updated Today</span>
          </div>
          <div class="updated-time">⏱️ Updated today</div>
          <div class="script-meta"><span>👁️ 2.3K views</span><span>⭐ 4.8 rating</span></div>
          <div class="code">loadstring(game:HttpGet('https://raw.githubusercontent.com/rysscripts/adoptme/refs/heads/main/loader.lua'))()</div>
          <div class="btn-row">
            <button class="btn copy-btn" data-script-name="Adopt Me Script" data-copy="loadstring(game:HttpGet('https://raw.githubusercontent.com/rysscripts/adoptme/refs/heads/main/loader.lua'))()">Copy Script</button>
            <button class="btn secondary open-modal">Quick View</button>
          </div>
        </article>

        <article class="card glass spotlight searchable"
          data-name="pet simulator 99 script in development"
          data-category="script"
          data-title="Pet Simulator 99 Script"
          data-detail="Pet Simulator 99 Script is currently in development. No public loader yet."
          data-copy="Pet Simulator 99 Script is currently in development.">
          <div class="card-badges-top"><span class="micro-badge">NEW</span><span class="micro-badge trending">TRENDING</span></div>
          <div class="top-row">
            <div class="app-icon">
              <img src="images/ps99.png" alt="Pet Simulator 99 Script" onerror="this.style.display='none'; this.parentElement.innerHTML='<div class=&quot;icon-letter&quot;>P9</div>';">
            </div>
            <span class="pill">PS99</span>
          </div>
          <div class="status"><span class="dot"></span> In Development 🛠️</div>
          <h3>Pet Simulator 99 Script</h3>
          <div class="badge-row">
            <span class="badge dev">In Development 🛠️</span>
            <span class="badge">Soon</span>
          </div>
          <div class="updated-time">⏱️ Updated recently</div>
          <div class="script-meta"><span>👁️ 0.7K views</span><span>⭐ 4.6 hype</span></div>
          <div class="code">Pet Simulator 99 Script is currently in development.</div>
          <div class="btn-row">
            <button class="btn copy-btn" data-script-name="Pet Simulator 99 Script" data-copy="Pet Simulator 99 Script is currently in development.">Copy Script</button>
            <button class="btn secondary open-modal">Quick View</button>
          </div>
        </article>

        <article class="card glass spotlight searchable"
          data-name="steal a brainrot script"
          data-category="script"
          data-title="Steal A Brainrot Script"
          data-detail="https://pastefy.app/PeXTBlNh/raw"
          data-copy="https://pastefy.app/PeXTBlNh/raw">
          <div class="card-badges-top"><span class="micro-badge">NEW</span><span class="micro-badge trending">TRENDING</span></div>
          <div class="top-row">
            <div class="app-icon">
              <img src="images/brainrot.png" alt="Steal A Brainrot Script" onerror="this.style.display='none'; this.parentElement.innerHTML='<div class=&quot;icon-letter&quot;>SB</div>';">
            </div>
            <span class="pill">SAB</span>
          </div>
          <div class="status"><span class="dot"></span> Working</div>
          <h3>Steal A Brainrot Script</h3>
          <div class="badge-row">
            <span class="badge version">Raw</span>
            <span class="badge">Verified</span>
            <span class="badge">Updated Today</span>
          </div>
          <div class="updated-time">⏱️ Updated today</div>
          <div class="script-meta"><span>👁️ 1.1K views</span><span>⭐ 4.7 rating</span></div>
          <div class="code">https://pastefy.app/PeXTBlNh/raw</div>
          <div class="btn-row">
            <button class="btn copy-btn" data-script-name="Steal A Brainrot Script" data-copy="https://pastefy.app/PeXTBlNh/raw">Copy Script</button>
            <button class="btn secondary open-modal">Quick View</button>
          </div>
        </article>

        <article class="card glass spotlight searchable"
          data-name="blade ball script"
          data-category="script"
          data-title="Blade Ball Script"
          data-detail="loadstring(game:HttpGet('https://pastefy.app/6Mzmod71/raw'))()"
          data-copy="loadstring(game:HttpGet('https://pastefy.app/6Mzmod71/raw'))()">
          <div class="card-badges-top"><span class="micro-badge">NEW</span><span class="micro-badge trending">TRENDING</span></div>
          <div class="top-row">
            <div class="app-icon">
              <img src="images/bladeball.png" alt="Blade Ball Script" onerror="this.style.display='none'; this.parentElement.innerHTML='<div class=&quot;icon-letter&quot;>BB</div>';">
            </div>
            <span class="pill">BB</span>
          </div>
          <div class="status"><span class="dot"></span> Working</div>
          <h3>Blade Ball Script</h3>
          <div class="badge-row">
            <span class="badge version">v1.0</span>
            <span class="badge">Verified</span>
            <span class="badge">Updated Today</span>
          </div>
          <div class="updated-time">⏱️ Updated 1 hour ago</div>
          <div class="script-meta"><span>👁️ 1.5K views</span><span>⭐ 4.8 rating</span></div>
          <div class="code">loadstring(game:HttpGet('https://pastefy.app/6Mzmod71/raw'))()</div>
          <div class="btn-row">
            <button class="btn copy-btn" data-script-name="Blade Ball Script" data-copy="loadstring(game:HttpGet('https://pastefy.app/6Mzmod71/raw'))()">Copy Script</button>
            <button class="btn secondary open-modal">Quick View</button>
          </div>
        </article>
      </div>
    </section>

    <section class="reveal" id="executors">
      <div class="section-head">
        <h2><span style="color: var(--primary-3);">Premium</span> Executors</h2>
        <div class="line"></div>
      </div>

      <div class="grid cards-2">
        <article class="card glass spotlight searchable"
          data-name="delta mobile executor android mobile"
          data-category="executor"
          data-title="DELTA MOBILE"
          data-detail="https://deltaexploits.gg/">
          <div class="card-badges-top"><span class="micro-badge">NEW</span><span class="micro-badge trending">TRENDING</span></div>
          <div class="top-row">
            <div class="app-icon">
              <img src="images/delta.png" alt="Delta Mobile" onerror="this.style.display='none'; this.parentElement.innerHTML='<div class=&quot;icon-letter&quot;>D</div>';">
            </div>
            <span class="pill">DELTA</span>
          </div>
          <div class="status undetected"><span class="dot"></span> Undetected</div>
          <h3>DELTA MOBILE</h3>
          <div class="badge-row">
            <span class="badge version">Android</span>
            <span class="badge">Verified</span>
            <span class="badge">Fast</span>
          </div>
          <div class="updated-time">⏱️ Updated today</div>
          <div class="trust-row">
            <span class="trust-chip">✔ Verified</span>
            <span class="trust-chip">🛡️ Safe</span>
            <span class="trust-chip">⚡ Fast Injection</span>
          </div>
          <div class="script-meta"><span>👁️ 2.6K views</span><span>🟢 Undetected</span></div>
          <div class="code">https://deltaexploits.gg/</div>
          <div class="btn-row">
            <a class="btn external-link" href="https://deltaexploits.gg/" target="_blank" rel="noopener noreferrer">Download Now</a>
            <button class="btn secondary open-modal">Quick View</button>
          </div>
        </article>

        <article class="card glass spotlight searchable"
          data-name="delta ios executor ios mobile"
          data-category="executor"
          data-title="DELTA IOS"
          data-detail="https://deltaexploits.gg/">
          <div class="card-badges-top"><span class="micro-badge">NEW</span><span class="micro-badge trending">TRENDING</span></div>
          <div class="top-row">
            <div class="app-icon">
              <img src="images/delta-ios.png" alt="Delta iOS" onerror="this.style.display='none'; this.parentElement.innerHTML='<div class=&quot;icon-letter&quot;>DI</div>';">
            </div>
            <span class="pill">DELTA IOS</span>
          </div>
          <div class="status testing"><span class="dot"></span> Testing</div>
          <h3>DELTA IOS</h3>
          <div class="badge-row">
            <span class="badge version">iOS</span>
            <span class="badge">Mobile</span>
            <span class="badge">Smooth</span>
          </div>
          <div class="updated-time">⏱️ Updated today</div>
          <div class="trust-row">
            <span class="trust-chip">✔ Verified</span>
            <span class="trust-chip">🛡️ Safe</span>
            <span class="trust-chip">⚡ Smooth UI</span>
          </div>
          <div class="script-meta"><span>👁️ 2.1K views</span><span>🟡 Testing</span></div>
          <div class="code">https://deltaexploits.gg/</div>
          <div class="btn-row">
            <a class="btn external-link" href="https://deltaexploits.gg/" target="_blank" rel="noopener noreferrer">Download Now</a>
            <button class="btn secondary open-modal">Quick View</button>
          </div>
        </article>

        <article class="card glass spotlight searchable"
          data-name="solara executor windows"
          data-category="executor"
          data-title="SOLARA Executor"
          data-detail="https://getsolara.dev/">
          <div class="card-badges-top"><span class="micro-badge">NEW</span><span class="micro-badge trending">TRENDING</span></div>
          <div class="top-row">
            <div class="app-icon">
              <img src="images/solara.png" alt="Solara Executor" onerror="this.style.display='none'; this.parentElement.innerHTML='<div class=&quot;icon-letter&quot;>S</div>';">
            </div>
            <span class="pill">SOLARA</span>
          </div>
          <div class="status undetected"><span class="dot"></span> Undetected</div>
          <h3>SOLARA Executor</h3>
          <div class="badge-row">
            <span class="badge version">Windows</span>
            <span class="badge">Verified</span>
            <span class="badge">Stable</span>
          </div>
          <div class="updated-time">⏱️ Updated today</div>
          <div class="trust-row">
            <span class="trust-chip">✔ Verified</span>
            <span class="trust-chip">🛡️ Stable</span>
            <span class="trust-chip">⚡ Windows Ready</span>
          </div>
          <div class="script-meta"><span>👁️ 1.9K views</span><span>🟢 Undetected</span></div>
          <div class="code">https://getsolara.dev/</div>
          <div class="btn-row">
            <a class="btn external-link" href="https://getsolara.dev/" target="_blank" rel="noopener noreferrer">Download Now</a>
            <button class="btn secondary open-modal">Quick View</button>
          </div>
        </article>
      </div>
    </section>

    <section class="reveal" id="socials">
      <div class="section-head">
        <h2>Socials &amp; Support</h2>
        <div class="line"></div>
      </div>

      <div class="grid cards-2 socials-grid" style="grid-template-columns: repeat(2, minmax(0, 1fr));">
        <article class="card glass spotlight social-card">
          <div class="social-icon-wrap youtube">
            <svg viewBox="0 0 24 24" width="40" height="40" fill="#ff4e4e" aria-hidden="true"><path d="M21.8 8s-.2-1.6-.8-2.3c-.8-.9-1.7-.9-2.1-1C15.9 4.5 12 4.5 12 4.5h0s-3.9 0-6.9.2c-.4.1-1.3.1-2.1 1C2.4 6.4 2.2 8 2.2 8S2 9.9 2 11.9v1.8c0 2 .2 3.9.2 3.9s.2 1.6.8 2.3c.8.9 1.9.9 2.4 1 1.7.2 6.6.2 6.6.2s3.9 0 6.9-.2c.4-.1 1.3-.1 2.1-1 .6-.7.8-2.3.8-2.3s.2-1.9.2-3.9v-1.8C22 9.9 21.8 8 21.8 8ZM9.5 15.9V8.8l6.3 3.6-6.3 3.5Z"/></svg>
          </div>
          <h3 style="margin-bottom:8px;">YouTube</h3>
          <p class="muted" style="margin:0 0 24px;">Latest showcases</p>
          <div class="social-btn-row">
            <a class="btn external-link" href="https://www.youtube.com/@kjcscripts" target="_blank" rel="noopener noreferrer">Subscribe</a>
          </div>
        </article>

        <article class="card glass spotlight social-card">
          <div class="social-icon-wrap tiktok">
            <svg viewBox="0 0 24 24" width="40" height="40" fill="#ff4db8" aria-hidden="true"><path d="M16.6 5.8c1 .8 2.1 1.3 3.4 1.4v3a8.8 8.8 0 0 1-3.4-.8v5.2a5.7 5.7 0 1 1-5.7-5.7c.4 0 .8 0 1.1.1v3.1a2.6 2.6 0 1 0 1.5 2.4V2h3.1c.2 1.5.9 2.8 2 3.8Z"/></svg>
          </div>
          <h3 style="margin-bottom:8px;">TikTok</h3>
          <p class="muted" style="margin:0 0 24px;">Clips &amp; Shorts</p>
          <div class="social-btn-row">
            <a class="btn external-link" href="https://www.tiktok.com/@vertexscripts" target="_blank" rel="noopener noreferrer">Follow Me</a>
          </div>
        </article>
      </div>
    </section>
  </main>

  <div class="admin-modal" id="adminModal">
    <div class="glass admin-modal-card">
      <button class="close-btn" id="closeAdminModal">✕</button>
      <section class="admin-panel" id="admin-panel">
        <div class="section-head">
          <h2><span style="color: var(--primary-3);">Owner</span> Dashboard</h2>
          <div class="line"></div>
        </div>

        <div class="glass" style="padding:24px; margin-bottom:20px;">
          <p class="admin-auth-note">
            Log in with your Supabase account to view real visitor stats and copy counts.
            This works on GitHub Pages and is already configured with your Supabase project.
          </p>

          <div class="admin-form">
            <div class="admin-pin-row">
              <input type="password" id="adminPin" placeholder="Owner PIN code" autocomplete="one-time-code" />
            </div>
            <input type="email" id="adminEmail" placeholder="Admin email" autocomplete="username" />
            <input type="password" id="adminPassword" placeholder="Admin password" autocomplete="current-password" />
            <div class="btn-row" style="justify-content:flex-start;">
              <button class="btn" id="adminLoginSubmit">Log In</button>
              <button class="btn secondary" id="adminLogoutBtn" style="display:none;">Log Out</button>
            </div>
            <div class="admin-loading" id="adminLoading"><span class="spinner"></span><span>Checking access...</span></div>
            <div class="admin-helper" id="adminStatusText">Supabase is configured. Enter your PIN and log in to view real stats.</div>
          </div>
        </div>

        <div class="admin-grid" id="adminGrid">
          <div class="admin-stat glass"><strong id="statVisitors">0</strong><span>Total visitors</span></div>
          <div class="admin-stat glass"><strong id="statCopies">0</strong><span>Total script copies</span></div>
          <div class="admin-stat glass"><strong id="statUnique">0</strong><span>Unique sessions</span></div>
          <div class="admin-stat glass"><strong id="statTop">—</strong><span>Top script</span></div>
        </div>

        <div class="glass" id="adminBreakdownWrap" style="padding:24px;">
          <h3 style="margin-top:0;">Script copy breakdown</h3>
          <div class="admin-list" id="adminBreakdown">
            <div class="admin-row"><span>MM2 Script</span><strong>0</strong></div>
            <div class="admin-row"><span>Adopt Me Script</span><strong>0</strong></div>
            <div class="admin-row"><span>Steal A Brainrot Script</span><strong>0</strong></div>
            <div class="admin-row"><span>Blade Ball Script</span><strong>0</strong></div>
            <div class="admin-row"><span>Pet Simulator 99 Script</span><strong>0</strong></div>
          </div>
        </div>
      </section>
    </div>
  </div>

  <footer class="wrap reveal" style="padding-bottom:44px;">
    <div class="glass footer-brand">
      <strong>Made by xyz 👑</strong>
      <div>Trusted &amp; Verified Scripts</div>
      <small>© 2026 Vertex Hub</small>
    </div>
  </footer>

  <div class="live-feed">
    <div class="live-feed-item" id="liveFeedItem">🔥 @Masonhub copied MM2 Script</div>
  </div>

  <div class="toast" id="toast">
    <div class="toast-badge">📋</div>
    <div><strong>Copied Successfully ✨</strong><br><span id="toastText">Script copied to clipboard.</span></div>
  </div>

  <button class="back-top" id="backTop" aria-label="Back to top">↑</button>

  <div class="modal" id="modal">
    <div class="modal-card glass">
      <button class="close-btn" id="closeModal">✕</button>
      <div class="pill" style="margin-bottom:14px;">Quick View</div>
      <h3 id="modalTitle" style="margin-top:0;">Title</h3>
      <p id="modalDetail" class="muted">Details go here.</p>
      <a id="modalLink" class="modal-link" href="#" target="_blank" rel="noopener noreferrer" style="display:none;">
        <span id="modalLinkText">Open link</span><span>↗</span>
      </a>
      <div class="btn-row" style="margin-top:18px;">
        <button class="btn" id="modalCopy">Copy Script</button>
        <button class="btn secondary" id="modalDismiss">Close</button>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script>
    const body = document.body;
    const navLinks = document.getElementById('navLinks');
    const menuBtn = document.getElementById('menuBtn');
    const themeToggle = document.getElementById('themeToggle');
    const searchInput = document.getElementById('searchInput');
    const filters = [...document.querySelectorAll('.tag-filter')];
    const cards = [...document.querySelectorAll('.searchable')];

    const toast = document.getElementById('toast');
    const toastText = document.getElementById('toastText');

    const backTop = document.getElementById('backTop');

    const modal = document.getElementById('modal');
    const modalTitle = document.getElementById('modalTitle');
    const modalDetail = document.getElementById('modalDetail');
    const modalDismiss = document.getElementById('modalDismiss');
    const closeModal = document.getElementById('closeModal');
    const modalCopy = document.getElementById('modalCopy');
    const modalLink = document.getElementById('modalLink');
    const modalLinkText = document.getElementById('modalLinkText');

    const reveals = document.querySelectorAll('.reveal');
    const counters = document.querySelectorAll('.counter');
    const heroVisitorCount = document.getElementById('heroVisitorCount');
    const liveFeedItem = document.getElementById('liveFeedItem');

    const adminBtn = document.getElementById('adminBtn');
    const adminModal = document.getElementById('adminModal');
    const closeAdminModal = document.getElementById('closeAdminModal');
    const adminEmail = document.getElementById('adminEmail');
    const adminPassword = document.getElementById('adminPassword');
    const adminPin = document.getElementById('adminPin');
    const adminLoginSubmit = document.getElementById('adminLoginSubmit');
    const adminLogoutBtn = document.getElementById('adminLogoutBtn');
    const adminLoading = document.getElementById('adminLoading');
    const adminStatusText = document.getElementById('adminStatusText');
    const statVisitors = document.getElementById('statVisitors');
    const statCopies = document.getElementById('statCopies');
    const statUnique = document.getElementById('statUnique');
    const statTop = document.getElementById('statTop');
    const adminBreakdown = document.getElementById('adminBreakdown');
    const adminGrid = document.getElementById('adminGrid');
    const adminBreakdownWrap = document.getElementById('adminBreakdownWrap');

    let activeFilter = 'all';
    let currentModalText = 'Vertex Scripts details';
    let toastTimer;
    let fakeVisitors = 248;
    let lightMode = false;
    let supabaseClient = null;

    const SUPABASE_URL = 'https://nveekossobmdrejhwrdj.supabase.co';
    const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im52ZWVrb3Nzb2JtZHJlamh3cmRqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQ2NDI4NzMsImV4cCI6MjA5MDIxODg3M30.KKGImnQ5WtBrjbDUkIX9OQE9tYhFaJOSxXSStUKw_sg';

    const SESSION_KEY = 'vertex_session_id';
    const ADMIN_PIN = '72841';
    const TRACK_COOLDOWN_MS = 6000;
    const COPY_COOLDOWN_PREFIX = 'vertex_copy_cooldown_';
    const SCRIPT_NAMES = ['MM2 Script', 'Adopt Me Script', 'Steal A Brainrot Script', 'Blade Ball Script', 'Pet Simulator 99 Script'];

    function showToast(message) {
      toastText.textContent = message;
      toast.classList.remove('show');
      void toast.offsetWidth;
      toast.classList.add('show');
      clearTimeout(toastTimer);
      toastTimer = setTimeout(() => toast.classList.remove('show'), 1800);
    }

    function getSessionId() {
      let id = localStorage.getItem(SESSION_KEY);
      if (!id) {
        id = `sess_${Math.random().toString(36).slice(2, 10)}${Date.now().toString(36)}`;
        localStorage.setItem(SESSION_KEY, id);
      }
      return id;
    }

    function initSupabase() {
      if (!window.supabase || !SUPABASE_URL || !SUPABASE_ANON_KEY) return null;
      return window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
    }

    async function trackEvent(type, scriptName = null) {
      if (!supabaseClient) return;

      if (type === 'copy' && scriptName) {
        const key = `${COPY_COOLDOWN_PREFIX}${scriptName}`;
        const lastAt = Number(localStorage.getItem(key) || '0');
        if (Date.now() - lastAt < TRACK_COOLDOWN_MS) return;
        localStorage.setItem(key, String(Date.now()));
      }

      try {
        await supabaseClient.from('site_events').insert({
          event_type: type,
          script_name: scriptName,
          session_id: getSessionId(),
          page_path: window.location.pathname,
          created_at: new Date().toISOString()
        });
      } catch (error) {
        console.warn('Tracking failed:', error.message);
      }
    }

    async function loadDashboardStats() {
      if (!supabaseClient) return;

      try {
        const { data: events, error } = await supabaseClient
          .from('site_events')
          .select('event_type, script_name, session_id');

        if (error) throw error;

        const totalVisitors = events.filter(e => e.event_type === 'page_view').length;
        const totalCopies = events.filter(e => e.event_type === 'copy').length;
        const uniqueSessions = new Set(events.map(e => e.session_id).filter(Boolean)).size;

        const scriptCounts = {};
        events
          .filter(e => e.event_type === 'copy' && e.script_name)
          .forEach(e => {
            scriptCounts[e.script_name] = (scriptCounts[e.script_name] || 0) + 1;
          });

        const topEntry = Object.entries(scriptCounts).sort((a, b) => b[1] - a[1])[0];

        statVisitors.textContent = String(totalVisitors);
        statCopies.textContent = String(totalCopies);
        statUnique.textContent = String(uniqueSessions);
        statTop.textContent = topEntry ? topEntry[0] : '—';

        adminBreakdown.innerHTML = SCRIPT_NAMES.map(name => `
          <div class="admin-row"><span>${name}</span><strong>${scriptCounts[name] || 0}</strong></div>
        `).join('');
      } catch (error) {
        adminStatusText.textContent = `Dashboard load failed: ${error.message}`;
      }
    }

    async function refreshAuthState() {
      if (!supabaseClient) return;

      const { data } = await supabaseClient.auth.getSession();
      const loggedIn = !!data.session;

      adminLogoutBtn.style.display = loggedIn ? 'inline-flex' : 'none';
      adminStatusText.textContent = loggedIn
        ? 'Connected. Real stats are loading from Supabase.'
        : 'Not logged in. Use your Supabase email and password.';

      if (loggedIn) {
        adminGrid.style.display = 'grid';
        adminBreakdownWrap.style.display = 'block';
        await loadDashboardStats();
      }
    }

    async function copyText(text, message = 'Script copied successfully ✨') {
      try {
        await navigator.clipboard.writeText(text);
      } catch {
        const area = document.createElement('textarea');
        area.value = text;
        document.body.appendChild(area);
        area.select();
        document.execCommand('copy');
        area.remove();
      }
      showToast(message);
    }

    function applyFilters() {
      const query = searchInput.value.trim().toLowerCase();

      cards.forEach(card => {
        const matchesSearch = (card.dataset.name || '').toLowerCase().includes(query);
        const matchesFilter = activeFilter === 'all' || card.dataset.category === activeFilter;
        card.classList.toggle('hidden', !(matchesSearch && matchesFilter));
      });
    }

    function updateLiveStatus() {
      const hour = new Date().getHours();
      let change = Math.floor(Math.random() * 5) - 2;

      if (hour >= 18 && hour <= 23) change += Math.floor(Math.random() * 4);
      if (hour >= 2 && hour <= 6) change -= Math.floor(Math.random() * 3);
      if (Math.random() < 0.08) change += Math.floor(Math.random() * 10) + 5;
      if (Math.random() < 0.05) change -= Math.floor(Math.random() * 8) + 3;

      fakeVisitors = Math.max(160, Math.min(520, fakeVisitors + change));

      if (heroVisitorCount) {
        const deltaClass = change >= 0 ? 'delta-up' : 'delta-down';
        const deltaArrow = change >= 0 ? '▲' : '▼';
        heroVisitorCount.innerHTML = `${fakeVisitors}+ <span class="delta-indicator ${deltaClass}">${deltaArrow} ${Math.abs(change)}</span>`;
      }
    }

    function closeModalFn() {
      modal.style.display = 'none';
    }

    function randomUsername() {
      const firstNames = ['Mason', 'Luna', 'Ethan', 'Nova', 'Sophie', 'Jayden', 'Milo', 'Avery', 'Logan', 'Chloe', 'Ryder', 'Hazel', 'Levi', 'Aria'];
      const endings = ['x', 'hub', 'plays', 'dev', 'rise', 'fx', 'zz', 'tv', 'live', 'pro'];
      const name = firstNames[Math.floor(Math.random() * firstNames.length)];
      const ending = endings[Math.floor(Math.random() * endings.length)];
      return `@${name}${ending}`;
    }

    function randomScript() {
      return SCRIPT_NAMES[Math.floor(Math.random() * SCRIPT_NAMES.length)];
    }

    function showLiveFeed() {
      if (!liveFeedItem) return;

      const messages = [
        `🔥 ${randomUsername()} copied ${randomScript()}`,
        `⭐ ${randomScript()} Updated`,
        `🛠️ ${randomScript()} Fixed`,
        `👀 ${randomUsername()} is viewing scripts`
      ];

      liveFeedItem.textContent = messages[Math.floor(Math.random() * messages.length)];
      liveFeedItem.classList.remove('show');
      void liveFeedItem.offsetWidth;
      liveFeedItem.classList.add('show');

      setTimeout(() => liveFeedItem.classList.remove('show'), 3200);
    }

    menuBtn.addEventListener('click', () => {
      navLinks.classList.toggle('open');
    });

    navLinks.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => navLinks.classList.remove('open'));
    });

    searchInput.addEventListener('input', applyFilters);

    filters.forEach(btn => {
      btn.addEventListener('click', () => {
        filters.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        activeFilter = btn.dataset.filter;
        applyFilters();
      });
    });

    document.querySelectorAll('.copy-btn').forEach(button => {
      button.addEventListener('click', async () => {
        await copyText(button.dataset.copy, 'Copied successfully ✨');
        await trackEvent('copy', button.dataset.scriptName || null);
        if (supabaseClient) await loadDashboardStats();

        const original = button.textContent;
        button.textContent = '✔ Copied';
        button.style.filter = 'brightness(1.08)';

        setTimeout(() => {
          button.textContent = original;
          button.style.filter = '';
        }, 1200);
      });
    });

    document.querySelectorAll('.open-modal').forEach(button => {
      button.addEventListener('click', event => {
        const card = event.currentTarget.closest('[data-title]');
        modalTitle.textContent = card?.dataset.title || 'Preview';
        modalDetail.textContent = card?.dataset.detail || 'Script details.';
        currentModalText = card?.dataset.copy || card?.dataset.detail || 'Script details.';

        const detail = card?.dataset.detail || '';
        const isLink = /^https?:\/\//.test(detail);

        if (isLink) {
          modalLink.href = detail;
          modalLinkText.textContent = detail;
          modalLink.style.display = 'inline-flex';
        } else {
          modalLink.style.display = 'none';
          modalLink.href = '#';
          modalLinkText.textContent = 'Open link';
        }

        modal.style.display = 'flex';
      });
    });

    modalDismiss.addEventListener('click', closeModalFn);
    closeModal.addEventListener('click', closeModalFn);

    modal.addEventListener('click', event => {
      if (event.target === modal) closeModalFn();
    });

    modalCopy.addEventListener('click', () => copyText(currentModalText, 'Copied successfully ✨'));

    const revealObserver = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add('visible');
      });
    }, { threshold: 0.16 });

    reveals.forEach(el => revealObserver.observe(el));

    const counterObserver = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        const el = entry.target;

        if (el.id === 'heroVisitorCount') return;

        const target = Number(el.dataset.target || 0);
        let value = 0;
        const step = Math.max(1, Math.ceil(target / 40));

        const tick = () => {
          value += step;
          if (value >= target) {
            el.textContent = target + (target === 98 ? '%' : '+');
          } else {
            el.textContent = value + (target === 98 ? '%' : '+');
            requestAnimationFrame(tick);
          }
        };

        tick();
        counterObserver.unobserve(el);
      });
    }, { threshold: 0.4 });

    counters.forEach(counter => counterObserver.observe(counter));

    window.addEventListener('scroll', () => {
      backTop.classList.toggle('show', window.scrollY > 500);

      const sections = [...document.querySelectorAll('main section, header.hero')];
      const links = [...document.querySelectorAll('.nav-links a')];
      let current = 'home';

      sections.forEach(section => {
        const top = section.offsetTop - 130;
        if (window.scrollY >= top) current = section.id || current;
      });

      links.forEach(link => {
        link.classList.toggle('active', link.getAttribute('href') === `#${current}`);
      });
    });

    backTop.addEventListener('click', () => {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });

    themeToggle.addEventListener('click', () => {
      lightMode = !lightMode;
      if (lightMode) {
        document.documentElement.style.setProperty('--text', '#122926');
        document.documentElement.style.setProperty('--muted', '#496e64');
        document.documentElement.style.setProperty('--muted-2', '#5e847a');
        document.body.style.background = 'linear-gradient(120deg, #effdf8 0%, #dff8ef 55%, #f5fffb 100%)';
        showToast('Light mode enabled.');
      } else {
        window.location.reload();
      }
    });

    adminBtn.addEventListener('click', async () => {
      if (!supabaseClient) {
        showToast('Admin unavailable ⚙️');
        return;
      }

      adminModal.classList.add('show');

      const { data } = await supabaseClient.auth.getSession();
      const loggedIn = !!data.session;

      if (!loggedIn) {
        adminGrid.style.display = 'none';
        adminBreakdownWrap.style.display = 'none';
        adminStatusText.textContent = 'Not logged in. Use your Supabase email and password.';
        return;
      }

      adminGrid.style.display = 'grid';
      adminBreakdownWrap.style.display = 'block';
      await loadDashboardStats();
    });

    closeAdminModal.addEventListener('click', event => {
      event.preventDefault();
      event.stopPropagation();
      adminModal.classList.remove('show');
    });

    adminModal.addEventListener('click', event => {
      if (event.target === adminModal) {
        adminModal.classList.remove('show');
      }
    });

    document.addEventListener('keydown', event => {
      if (event.key === 'Escape') {
        adminModal.classList.remove('show');
        closeModalFn();
      }
    });

    adminLoginSubmit.addEventListener('click', async () => {
      if (!supabaseClient) {
        showToast('Supabase is not connected ⚙️');
        return;
      }

      const pin = adminPin.value.trim();
      const email = adminEmail.value.trim();
      const password = adminPassword.value;

      if (!pin) {
        showToast('Enter your PIN code');
        return;
      }

      if (pin !== ADMIN_PIN) {
        adminStatusText.textContent = 'Incorrect PIN code.';
        showToast('Wrong PIN');
        return;
      }

      if (!email || !password) {
        showToast('Enter email and password');
        return;
      }

      adminLoading.classList.add('show');
      adminLoginSubmit.disabled = true;

      await new Promise(resolve => setTimeout(resolve, 1200));
      const { error } = await supabaseClient.auth.signInWithPassword({ email, password });

      adminLoading.classList.remove('show');
      adminLoginSubmit.disabled = false;

      if (error) {
        adminStatusText.textContent = `Login failed: ${error.message}`;
        return;
      }

      adminGrid.style.display = 'grid';
      adminBreakdownWrap.style.display = 'block';
      showToast('Admin login successful 👑');
      await refreshAuthState();
    });

    adminLogoutBtn.addEventListener('click', async () => {
      if (!supabaseClient) return;
      await supabaseClient.auth.signOut();
      adminStatusText.textContent = 'Logged out.';
      adminLogoutBtn.style.display = 'none';
      adminGrid.style.display = 'none';
      adminBreakdownWrap.style.display = 'none';
      adminPin.value = '';
      showToast('Logged out');
    });

    document.querySelectorAll('.spotlight').forEach(card => {
      card.addEventListener('mousemove', event => {
        const rect = card.getBoundingClientRect();
        const x = ((event.clientX - rect.left) / rect.width) * 100;
        const y = ((event.clientY - rect.top) / rect.height) * 100;
        card.style.setProperty('--sx', `${x}%`);
        card.style.setProperty('--sy', `${y}%`);
        const rotateY = ((event.clientX - rect.left) / rect.width - 0.5) * 8;
        const rotateX = -((event.clientY - rect.top) / rect.height - 0.5) * 8;
        card.style.transform = `translateY(-2px) rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;
      });

      card.addEventListener('mouseleave', () => {
        card.style.transform = '';
      });
    });

    document.addEventListener('mousemove', event => {
      const x = (event.clientX / window.innerWidth) * 100;
      const y = (event.clientY / window.innerHeight) * 100;
      body.style.setProperty('--mx', `${x}%`);
      body.style.setProperty('--my', `${y}%`);
    });

    document.body.style.animation = 'pageEnter .55s ease';

    supabaseClient = initSupabase();
    if (supabaseClient) {
      trackEvent('page_view');
      refreshAuthState();
      supabaseClient.auth.onAuthStateChange(() => {
        refreshAuthState();
      });
      adminStatusText.textContent = 'Supabase connected. Log in to view real stats.';
    }

    updateLiveStatus();
    setInterval(updateLiveStatus, 2600);
    setInterval(showLiveFeed, 7000);
    showLiveFeed();
    applyFilters();

    console.assert(typeof applyFilters === 'function', 'applyFilters should exist');
    console.assert(typeof updateLiveStatus === 'function', 'updateLiveStatus should exist');
    console.assert(document.getElementById('modal') !== null, 'modal should exist');
    console.assert(document.querySelectorAll('.searchable').length >= 5, 'should have searchable cards');
    console.assert(document.getElementById('liveFeedItem') !== null, 'live feed should exist');
    console.assert(document.getElementById('adminModal') !== null, 'admin modal should exist');
    console.assert(document.getElementById('modalLink') !== null, 'modal link should exist');
    console.assert(document.querySelectorAll('.external-link').length >= 5, 'external links should exist');
  </script>
</body>
</html>
